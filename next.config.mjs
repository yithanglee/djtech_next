/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api.qrserver.com', 'blog.damienslab.com', 'localhost'],  // Add localhost here
      },
};

export default nextConfig;
